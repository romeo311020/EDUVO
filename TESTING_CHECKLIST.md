# ✅ قائمة اختبار EDUVO

## 🔗 اختبار الاتصال بـ Supabase

### في Console البريقة (F12)

اختبر الاتصال بتشغيل هذا الكود:

```javascript
// 1. اختبر تحميل مكتبة Supabase
console.log('Supabase Loaded:', !!window.supabase);

// 2. اختبر الاتصال بقاعدة البيانات
const test = async () => {
  const { data, error } = await db.from('profiles').select('count');
  console.log('Connection Test:', { data, error });
};
test();

// 3. اختبر الجلسة الحالية
const checkSession = async () => {
  const { data: { session } } = await db.auth.getSession();
  console.log('Current Session:', session);
};
checkSession();
```

---

## 📱 الصفحات المتاحة

| الصفحة | الرابط | الوصف |
|-------|--------|--------|
| الرئيسية | `index.html` | تطبيق الطلاب الرئيسي |
| الإدارة | `admin.html` | لوحة التحكم |
| المطورين | `developer.html` | ⚠️ محفظة شخصية (يجب حذفها) |

---

## 🧪 الاختبارات الموصى بها

### 1️⃣ اختبار المصادقة
- [ ] التسجيل الجديد يعمل
- [ ] تسجيل الدخول يعمل
- [ ] تسجيل الخروج يعمل
- [ ] البيانات المحفوظة في Supabase

### 2️⃣ اختبار واجهة المستخدم
- [ ] الألوان صحيحة (المظهر الداكن)
- [ ] النصوص العربية تظهر بشكل صحيح
- [ ] التنقل بين الصفحات يعمل
- [ ] الأزرار تستجيب للضغط

### 3️⃣ اختبار لوحة الإدارة
- [ ] عرض قائمة الطلاب
- [ ] تحديث حالة الطالب
- [ ] البحث يعمل
- [ ] الإحصائيات تحدّث تلقائياً

### 4️⃣ اختبار الاختبارات
- [ ] تحميل الأسئلة من data.json
- [ ] الإجابة على الأسئلة
- [ ] حساب النتائج
- [ ] عرض النتيجة النهائية

### 5️⃣ اختبار الاستجابة
- [ ] الموقع يعمل على الهاتف
- [ ] الموقع يعمل على الجهاز اللوحي
- [ ] الموقع يعمل على سطح المكتب

---

## 🔧 تصحيح الأخطاء الشائعة

### المشكلة: رسالة "undefined" في Console

**السبب**: مكتبة Supabase لم تُحمّل بعد

**الحل**: انتظر تحميل الصفحة بالكامل قبل الاختبار

### المشكلة: "CORS Error"

**السبب**: نطاق الموقع لم يُضف في إعدادات Supabase

**الحل**: اذهب إلى Supabase Settings → API → CORS وأضف النطاق

### المشكلة: البيانات لا تُحفظ

**السبب**: جدول profiles لم يُنشأ أو صلاحيات Row Level Security لا تسمح بالكتابة

**الحل**: تأكد من تنفيذ جميع خطوات إعداد الجداول

---

## 📊 معلومات مفيدة

### مفاتيح البريقة
- اضغط **F12** لفتح Developer Tools
- اختر **Console** لرؤية الأخطاء

### تنظيف البيانات
```sql
-- حذف جميع الطلاب (احذر!)
DELETE FROM profiles WHERE role = 'student';

-- إعادة تعيين العدادات
TRUNCATE TABLE profiles RESTART IDENTITY CASCADE;
```

### مراقبة الحقيقية (Real-time)
```javascript
// استمع إلى التغييرات في الجدول
const subscription = db
  .on('*', { event: '*', schema: 'public', table: 'profiles' },
    payload => console.log('Change detected!', payload)
  )
  .subscribe();
```

---

## 🚀 التحسينات المستقبلية

- [ ] إضافة نظام الإشعارات
- [ ] تحسين سرعة التحميل
- [ ] إضافة رفع الملفات
- [ ] دعم الوضع الليلي المتقدم
- [ ] إضافة اختبارات الكاميرا
- [ ] تكامل مع Google/Facebook

---

## 📝 ملاحظات مهمة

⚠️ **لا تنسى:**
1. حفظ جميع التغييرات قبل الإغلاق
2. اختبار على أجهزة مختلفة
3. التحقق من رسائل الأخطاء في Console
4. حذف developer.html إذا لم تكن بحاجة له

✅ **تم تصحيح المشاكل التالية:**
- إضافة معالجة أخطاء شاملة
- تحسين رسائل التنبيه
- إضافة تتبع الاتصال
- دعم Real-time Updates
